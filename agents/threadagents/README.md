# Threadagents

This folder contains a minimal framework to spawn multiple thread-based agents
(`ThreadAgent`) and manage them with `AgentManager`. Use `run_agents.py` as a
simple example.

Usage:

```
python -m agents.threadagents.run_agents
```
