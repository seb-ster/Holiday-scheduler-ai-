# ML Agent

This folder contains a minimal ML agent scaffold: a `Trainer` that trains a
toy model and a `Model` placeholder. Use this as a starting point for
experiments and for opening a PR with a runnable MVP.

Usage:

```
python -m agents.ml_agent.run_ml_agent
```
